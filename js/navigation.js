document.addEventListener('DOMContentLoaded',()=>{const tabs=[...document.querySelectorAll('.tab')],pages=[...document.querySelectorAll('.tab-content')];function openTab(name){tabs.forEach(t=>t.classList.toggle('active',t.dataset.tab===name));pages.forEach(p=>p.classList.toggle('active',p.dataset.content===name));window.scrollTo({top:0,behavior:'smooth'});try{localStorage.setItem('67-active-tab',name)}catch(e){}}tabs.forEach(t=>t.addEventListener('click',()=>openTab(t.dataset.tab)));document.querySelectorAll('[data-go]').forEach(b=>b.addEventListener('click',()=>openTab(b.dataset.go)));const saved=localStorage.getItem('67-active-tab');if(saved&&pages.some(p=>p.dataset.content===saved))openTab(saved)});